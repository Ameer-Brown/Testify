<br>
<br>
<br>
<center><div class="container">
<%= form_tag seach_testimony_path, :method => 'get' do %>
  <p>
    <%= text_field_tag :search, params[:search] %>
    <%= submit_tag "Search", :name => nil %>
  </p>
<% end %>
</div></center>

<center><div class="container">
   <h1>Verses</h2>
   <div class="row">
     <%if @testimonies["content-length"] != "0"%>
     <% @testimonies["results"].each do |t| %>
       <div class="row">
        <div class="col s12 m7">
          <div class="card">
            <div class="card-image">
              <img src="https://images.unsplash.com/photo-1463003416389-296a1ad37ca0?ixlib=rb-0.3.5&q=80&fm=jpg&crop=entropy&s=1bed2a6743851633b655ae774c15ac07">
              <span class="card-title"><%=t["title"]%></span>
            </div>
            <div class="card-content">
            <%=t["preview"]%>
            </div>
            <div class="card-action">
                <!-- Hidden Form -->
                <%= form_tag create_testimony_path do |testimony| %>
                <div>
                    <%= testimony.hidden_field : %>
                </div>
                <div>
                    <%= testimony.hidden_field : %>
                </div>
                <%= f.submit "Save" %>
                <% end %>
              <a href="#">Save</a>
              <!-- End Hidden Form -->
            </div>
          </div>
        </div>
      </div>
     <% end %>
     <% end %>
   </div>
</div></center>
